<?php
/**
 * إجراء الحذف: إزالة المنتج بأمان وتنبيه المستخدم
 */

require_once 'includes/db.php';
require_once 'includes/functions.php';

// التحقق من صحة المعرف المعطى
if (isset($_GET['id']) && is_numeric($_GET['id'])) {
    $id = $_GET['id'];
    
    // محاولة الحذف
    if (delete_product($conn, $id)) {
        set_flash_message("success", "تم حذف المنتج بنجاح من قاعدة البيانات 🗑️");
    } else {
        set_flash_message("danger", "فشل الحذف، المنتج غير موجود أو مرتبط ببيانات أخرى.");
    }
} else {
    set_flash_message("warning", "عفواً، المعرف غير صالح.");
}

// العودة فوراً للصفحة الرئيسية
redirect("index.php");
?>
